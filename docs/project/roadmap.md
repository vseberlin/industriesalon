# Roadmap

Use `TODO.md` for active next actions, `backlog.md` for large future work, and `uat.md` for editor/UAT-dependent work. This file is for stable roadmap themes only.

## Themes

- Keep templates file-backed where practical and recover useful DB template edits deliberately.
- Consolidate shared relation, card, search, and archive contracts instead of adding parallel UI systems.
- Continue improving editor-visible Gutenberg workflows for events, projects, videos, register places, and archive/publication surfaces.
- Keep staging deployment Git-first and backed by explicit data artifacts.
