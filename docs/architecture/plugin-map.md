# Plugin Map

Compact ownership map for first-party plugins.

This file is boundary documentation, not an exhaustive API reference. It may
become stale in small details as plugins evolve; when a plugin contract changes,
update the affected row instead of expanding this into broad implementation
documentation.

| Plugin | Owns | Depends on | Exposes | Must not own | Important files |
| --- | --- | --- | --- | --- | --- |
| `iss-content` | shared CPT/editor/data contracts, Ausstellung date/type/programme/overview flags, Video CPT block data, Führung CPT/meta/block data | WordPress CPT/block APIs, occurrence-backed tour slots where block data needs public dates | content/video/tour dynamic blocks, editor support, `fuehrung` CPT, `fuehrung_typ` taxonomy, `wp iss-content videos`, `wp iss-content tours-drift-check` | public page skins owned by theme, booking writes, programme occurrence storage | `iss-content.php`, `includes/`, `blocks/`, `modules/tours/` |
| `iss-core` | shared infrastructure conventions: capabilities, admin grouping labels, schema option naming, debug logging, drift-result shapes | WordPress plugin/admin APIs | small helper functions | CPTs, REST routes, public renderers, assets, domain storage | `iss-core.php` |
| `iss-frontend` | shared frontend runtime helper conventions plus occurrence-backed programme/timeline rendering, tour calendar/date blocks, Ausstellung availability browser, and static atlas/map frontend renderers | `iss-occurrences`, WordPress enqueue/post/query APIs, `iss-commerce-lite` public read/write routes where frontend forms need endpoints, resolved place/config contracts from `iss-relations` | helper functions, `industriesalon/timeline-query`, `/iss/v1/timeline`, `/iss/v1/availability`, `industriesalon/ausstellungen-browser`, tour calendar/date blocks, static map renderer functions, `wp iss-frontend ausstellungen-audit` | source CPT contracts, occurrence storage, occurrence sync/mapping admin, relation/source resolution, theme visual skin, payment/order persistence | `iss-frontend.php`, `modules/programme/`, `modules/static-maps/` |
| `industriesalon-steuerung` | persistent institution facts: visit, address, contact, notices | WordPress options/meta/admin APIs | `industriesalon/field`, visit/contact render helpers | page-specific presentation | main plugin file, admin/readme files |
| `industriesalon-schoeneweide-register` | `register_place` structured data, epochs, state projection, interactive Atlas REST contracts, register caches, tools | WordPress CPT/meta, register tables/services | register blocks, Atlas REST payloads, admin tools, CLI guardrails | theme dossier composition, static editorial map-strip rendering | `includes/register-data/`, `includes/admin-tools.php`, `includes/cli.php` |
| `iss-archive` | archive ingest, normalization, projection, objects, collections, assertions, evidence | custom tables, source snapshots, WP posts | archive blocks/services, import/admin tools | graph-wide entity authority | `iss-archive.php`, `includes/*service.php`, `docs/` |
| `iss-graph` | entity-kind registry, offer subtype contract bridge and public subtype labels, entities, names, identifiers, evidence refs, relations, editorial signals for related/search/availability surfaces, profiles, transcript evidence bridge, SQL search projection, read-only `/iss/v1` facade including entity-scoped relations/occurrences | custom graph/search tables, WP posts, `iss-occurrences` query API when active | resolver, resolver-backed label creation, search provider, graph/profile services, editorial signal controls, public facade contract and graph routes | theme search UI composition, occurrence storage, offer/editorial CPT storage, JSON-LD rendering | `includes/entity-kinds.php`, `includes/core.php`, `includes/facade-rest.php`, `includes/search-*`, `includes/editorial-signals-*` |
| `iss-occurrences` | public programme occurrence projection, recurrence series, series/tag metadata, source-provider projection, SuperSaaS settings/API fetch/hourly occurrence sync, public tour-slot read adapter, indexed programme query API keyed by `source_post_id` / `source_post_type`, SuperSaaS series mapping/sync admin | WP source CPTs, SuperSaaS API sync data | `iss_occurrences_query()`, editorial source providers, SuperSaaS projection helper, `/iss/v1/tour-slots`, Tools sync/mapping/admin settings, CLI verify/sync/drift checks, `iss_supersaas_sync_occurrences()` | editorial content, public skins, graph/search authority or graph ID storage, booking/order writes | `includes/service.php`, `includes/providers.php`, `includes/providers/`, `includes/supersaas/`, `includes/admin-sync-page.php`, `includes/cli.php` |
| `iss-commerce-lite` | lightweight booking/order request capture, spam/rate-limit checks, durable request table, admin review/export/status handling, opt-in request notifications | WordPress REST APIs, publication sale meta, occurrence-backed tour-slot reads where booking forms need dates | `/is-tours/v1/book`, `/iss-payments/v1/publication-order`, booking/order action hooks, `Tools > ISS Anfragen`, `wp iss-commerce-lite verify` | SuperSaaS ingestion/settings, hidden calendar CPT storage, public skins, programme query authority, payment-provider settlement, catalog pricing authority, public templates | `iss-commerce-lite.php`, `includes/`, `assets/` |
| `iss-relations` | relation queries, relation-aware blocks, map-block source/place-selection contracts | WP posts/meta/terms, graph/register data where needed, `iss-frontend` for static map frontend rendering when active | related-content blocks/renderers, resolved place items for static map renderers | unrelated card skins, static map presentation ownership | `includes/`, `blocks/` |
| `iss-newsletter` | adapter between theme blocks and The Newsletter Plugin | The Newsletter Plugin tables/forms | `iss/newsletter-form` integration | parallel newsletter system | `iss-newsletter.php`, `blocks/` |

## Schema Behavior

- Custom tables must be plugin-owned, versioned with schema options, installed with `dbDelta()`, and accessed through service classes.
- Migration or projection state should be explicit and documented through artifacts or service methods, not hidden template behavior.
